#pragma once
// Firmware version — bump the patch number before each upload so the editor
// can confirm which build is running.
// Format: MAJOR.MINOR.PATCH
//   MAJOR — protocol / API breaking change
//   MINOR — feature step (3 = B-step 3, dynamic audio graph)
//   PATCH — increment each time you flash a new build
#define FW_VERSION "0.5.47"
