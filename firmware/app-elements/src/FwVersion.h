#pragma once
// app-elements firmware version. Bump PATCH before each flash.
// 0.x = spike. 0.1.0 = genuine Mutable Instruments Elements DSP vendored and
// driven via elements::Part (32 kHz -> 44.1 kHz resampled).
#define FW_VERSION "0.2.0"
