# Protocol schemas (placeholder)

JSON Schemas for the patch formats and the JSON-RPC API surface will live here. To be filled in as `core/Patch.h`'s blob schemas crystallise per application (see ADR 0005).

Planned files:
- `patch.effect.v1.json`        — project 1 patch
- `patch.amp.v1.json`           — project 2 patch
- `patch.synth.v1.json`         — project 3 patch
- `api.jsonrpc.v1.json`         — editor ↔ device JSON-RPC methods
